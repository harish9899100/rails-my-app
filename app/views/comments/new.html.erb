<h1>New Comment</h1>

<%= form_with model: @comment do |f| %>
  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.check_box :remember_name %>
    <%= f.label :remember_name, "Remember my name" %>
  </div>

  <%= f.submit "Create Comment" %>
<% end %>
