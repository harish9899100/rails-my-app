<h1>Items</h1>

<% if @items.any? %>
  <ol><br><br>
    <% @items.each do |item| %>
      <li>
        <%= link_to item.name, item_path(item) %>
      </li>
    <% end %>
  </ol>
<% else %>
  <p>No items found.</p>
<% end %>
<%#= render "table"%>

